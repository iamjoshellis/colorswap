import styled from 'styled-components';

const Wrap = styled.span`
  display: inline-block;
  width: 100%;
  position: relative;
  max-width: 300px;
  &:first-child {
    margin-right: 1rem;
  }
`;

export default Wrap;
